CREATE TABLE SECURITY (
  ID int NOT NULL,
  bvn bigint,
  SQ1 varchar(255),
  SA1 varchar(255),
  SQ2 varchar(255),
  SA2 varchar(255),
  SQ3 varchar(255),
  SA3 varchar(255),
  Primary Key(ID)
);

INSERT INTO SECURITY (ID) VALUES (1);
